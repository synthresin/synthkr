<h1>Posts</h1>
<table class="table table-hover">
	<% @posts.each do |post| %>
	<tr>
		<td><%= link_to post.title, admin_post_path(post) %><%=  %></td>
		<td class="del">삭제</td>
	</tr>
	<% end %>
</table>
<%= link_to "새 포스트",  new_admin_post_path, :class => 'btn btn-primary' %>
